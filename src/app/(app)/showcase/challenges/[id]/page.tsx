import type { Metadata } from "next";
import Link from "next/link";
import { notFound, redirect } from "next/navigation";
import { Trophy } from "lucide-react";
import { Topbar } from "@/components/layout/topbar";
import { ChallengeJoinPanel } from "@/components/challenges/challenge-join-panel";
import { ChallengeMatchCard } from "@/components/challenges/challenge-match-card";
import { ShowcaseCard } from "@/components/showcase/showcase-card";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { getCurrentUser } from "@/lib/auth";
import { getRequestLocale } from "@/lib/site-server";
import { labelsFor } from "@/lib/constants-i18n";
import { getCrewById, getMembershipCrewForUser } from "@/server/data/crews";
import { getChallenge, getChallengeParticipantCount, getChallengeParticipation, listChallengeMatches, listShowcaseEntries } from "@/server/data/showcase";

export async function generateMetadata(): Promise<Metadata> { const locale = await getRequestLocale(); return { title: locale === "en" ? "Build Challenge - BuildCrew" : "Build Challenge - BuildCrew" }; }
export default async function ChallengePage({ params }: { params: Promise<{ id: string }> }) {
  const locale = await getRequestLocale(); const copy = (pl: string, en: string) => locale === "en" ? en : pl; const labels = labelsFor(locale);
  const user = await getCurrentUser(); if (!user) redirect("/login"); const { id } = await params;
  const challenge = await getChallenge(id); if (!challenge) notFound();
  const [participation, count, crewId, entries] = await Promise.all([getChallengeParticipation(id, user.id), getChallengeParticipantCount(id), getMembershipCrewForUser(user.id), listShowcaseEntries({ challengeId: id, tab: "popular", viewerId: user.id })]);
  const crew = crewId ? await getCrewById(crewId) : null;
  const matches = participation?.mode === "FIND_CREW" && !participation.crewId ? await listChallengeMatches(id, user.id, locale) : [];
  const localeCode = locale === "en" ? "en-US" : "en-US";
  return <div><Topbar title={challenge.title} subtitle={challenge.prompt} /><div className="grid gap-6 lg:grid-cols-[1fr_340px]"><div className="space-y-6"><Card className="p-6"><div className="flex flex-wrap items-center gap-2"><Badge variant="warning">{labels.challengeStatuses[challenge.status]}</Badge>{challenge.category ? <Badge variant="outline">{challenge.category}</Badge> : null}<span className="text-[13px] text-neutral-400">{count} {copy(count === 1 ? "participant" : "participants", count === 1 ? "participant" : "participants")}</span></div>{challenge.description ? <p className="mt-4 whitespace-pre-wrap text-sm leading-7 text-neutral-600 dark:text-neutral-300">{challenge.description}</p> : null}<p className="mt-4 text-[13px] text-neutral-400">{copy("Build window", "Build window")}: {challenge.startsAt.toLocaleString(localeCode)} → {challenge.endsAt.toLocaleString(localeCode)}</p></Card>{["OPEN","BUILDING"].includes(challenge.status) ? <ChallengeJoinPanel challengeId={id} participation={participation ? { mode: participation.mode, crewId: participation.crewId } : null} crews={crew ? [{ id: crew.id, label: `Crew · ${crew.members.map((member) => member.profile.username).join(", ")}` }] : []} /> : null}{matches.length ? <div><h2 className="text-lg font-semibold">{copy("Best matches for your team", "Best matches for your team")}</h2><p className="mt-1 text-sm text-neutral-500">{copy("This is not random. We consider roles, interests, availability, goals and experience level.", "This is not random. We consider roles, interests, availability, goals and experience level.")}</p><div className="mt-4 grid gap-4 sm:grid-cols-2">{matches.map((match) => <ChallengeMatchCard key={match.profile.userId} challengeId={id} candidate={{ userId: match.profile.userId, username: match.profile.username, avatarEmoji: match.profile.avatarEmoji, role: match.profile.role, score: match.score, reasons: match.reasons }} />)}</div></div> : null}<div><div className="flex items-center justify-between gap-3"><div><h2 className="flex items-center gap-2 text-lg font-semibold"><Trophy className="h-5 w-5 text-amber-500" /> {copy("Challenge projects", "Challenge projects")}</h2><p className="text-sm text-neutral-500">{copy("Ranking is based on community reactions and feedback.", "Ranking is based on community reactions and feedback.")}</p></div>{participation && ["BUILDING","VOTING"].includes(challenge.status) ? <Button asChild size="sm"><Link href="/showcase/new">{copy("Submit project", "Submit project")}</Link></Button> : null}</div><div className="mt-4 grid gap-4 md:grid-cols-2">{entries.map((entry, index) => <div key={entry.id} className="relative">{challenge.status === "CLOSED" || challenge.status === "VOTING" ? <span className="absolute left-3 top-3 z-10 rounded-full bg-neutral-950/80 px-2.5 py-1 text-[13px] font-semibold text-white">#{index + 1}</span> : null}<ShowcaseCard currentUserId={user.id} entry={{ ...entry, viewerReactions: Array.from(entry.viewerReactions) }} /></div>)}</div>{!entries.length ? <p className="mt-6 rounded-[6px] border border-dashed p-8 text-center text-sm text-neutral-400">{copy("No team has published a result yet.", "No team has published a result yet.")}</p> : null}</div></div><div className="space-y-4"><Card className="p-5"><h3 className="font-semibold">{copy("Challenge goal", "Challenge goal")}</h3><p className="mt-2 text-sm text-neutral-500">{copy("The goal is not a perfect product. It's to form a team and ship something you can show.", "The goal is not a perfect product. It's to form a team and ship something you can show.")}</p></Card><Button asChild variant="outline" className="w-full"><Link href="/showcase/challenges">← {copy("All challenges", "All challenges")}</Link></Button></div></div></div>;
}
