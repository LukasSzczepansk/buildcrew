"use client";

/**
 * Kept as a no-op compatibility component so older imports remain valid.
 * BuildCrew now has one product language: English.
 */
export function LanguageSwitcher(_props: { compact?: boolean; className?: string }) {
  return null;
}
